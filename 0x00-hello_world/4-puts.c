#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    char my_list[100]={"\"Programming is like building a multilingual puzzle"};
    puts(my_list);
    return (0);
}
