#include <stdio.h>
#include "main.h"

/**
 * _strlen - gives length of string
 * @s: argument/input for this function
 * Return: 0 - when success
 */
int _strlen(char *s)
{
	int cont = 0;

	while (s[count] != '\0')
	{
		count++;
	}
	return (count);
}
